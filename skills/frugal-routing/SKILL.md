---
name: frugal-routing
description: Inspect and explain OpenCode Frugal model routing, worker roles, guardrails, and task escalation. Use when asked about routing status, model delegation, worker selection, or frugal behavior.
---

# OpenCode Frugal Routing

Use the active OpenCode agent inventory and this plugin's README as evidence.
Explain which exact model handles each role, which permissions constrain it,
and why a proposed task should stay deterministic, run in the orchestrator, or
be delegated.

Do not change routing configuration unless the user explicitly requests it.
